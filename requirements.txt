# Minimal dependencies for serverless
chardet>=5.0.0,<6.0

# Lightweight alternatives
# networkx>=3.0,<4.0  # Remove heavy dependency
# ast-monitor>=0.4.0  # Remove if not essential

# Keep essential only
requests>=2.28.0,<3.0
python-multipart>=0.0.5

# Add lightweight JSON/data handling
dataclasses-json>=0.5.0,<1.0
